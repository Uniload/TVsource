// TextureSwapStaticMesh
// A static mesh actor that can have its skins changed dynamically. A static mesh with bStatic = false
class TextureSwapStaticMesh extends StaticMeshActor;

defaultproperties
{
	bStatic = false
}